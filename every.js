// método every todos os elementos precisam atender a lógia para retornar true

const array = [1, 1, 1, 1];

console.log(array.every(element => element == 1));

console.log(array.every(element => element == 'a'));