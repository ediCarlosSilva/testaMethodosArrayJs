// some - retorna true se tiver pelo menos uma ocorrência do elmento testado.

const array = [1, 2, 3, 4];

console.log(array.some(element => element == 1));

console.log(array.some(element => element == 'a'));
